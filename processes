
while True:
    user_x = int(input('Enter the x (0-2): '))
    if user_x > 2:
        print('!')
    user_y = int(input('Enter the y (0-2): ')) 
    if user_y > 2:
        print('!')
    field[user_x][user_y] = '[X]'
    bot_move()
    show_field(field)
    if winner():
        break
